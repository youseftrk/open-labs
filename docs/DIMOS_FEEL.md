# Labs ← DimOS feel (clean-room notes)

Public references only. No Dimensional / DimOS code or marks.

## What to steal craft from
- **Console / arcade shell** — dimensional.org loads as a terminal/console setup (`SETTING COLOR : {#181919; #B0E1F0}`).
- **Ink + ice cyan** — charcoal ink `#181919` with ice cyan `#B0E1F0` as the operator signal (not SaaS blue).
- **Split operator layout** — command center (~30%+) beside a spatial/map or Rerun viewport; draggable divider; near-black `#0a0a0f` dashboard chrome in their web templates.
- **Spatial mapping as the stage** — costmap / pose / path / twin world is the primary surface; terminal/logs are the instrument docked to it.
- **Utilitarian robotics dashboard** — Hz, pose, connection state; sparse chrome.

## What we keep as ours
- Spatial glass / liquid-glass language from our design library (depth, hairline, 3 elevations).
- Startup brand craft from `startup-brand-identity` pack (shared across Design Friends).
- Labs product voice: “terminal for robot people — sim, train, deploy.”

## Open design decision
Reconcile Labs v0 acid green `#B8FF3C` vs DimOS ice cyan `#B0E1F0` — Design Engineer to recommend dual-signal or single lock.

## Accent lock (2026-09-22)
**Dual-signal**
- Console / chrome / idle telemetry: ice cyan `#B0E1F0`
- Live run / success / active train: acid green `#B8FF3C`
- Ink surface: `#171819` / `#181919` family (never pure `#000`)
