[package]
name = "labs"
version = "0.1.0"
description = "A terminal for robot people — sim, train, and deploy"
authors = ["Labs contributors"]
edition = "2021"
license = "Apache-2.0"

[lib]
name = "labs_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
opt-level = 3
panic = "abort"
strip = true
